{
  "name": "ses-lockdown",
  "version": "1.4.0",
  "private": true,
  "description": "Tether Secure Browser v1.4.0 (security hardening pass: adds attestSystemCheck, a genuine main-process-signed client attestation for the SYSTEM_CHECK flow using an embedded private key never exposed to the renderer, on top of v1.3.0's getClientVersion/getOperatingSystemInfo/getDisplayTopology/getSecureClientCapabilities read-only preload methods and v1.2.2's real verified-session establishment via attestation submission)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "npm run build && electron .",
    "pack": "npm run build && electron-builder --dir && npm run verify:package",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win && npm run verify:package -- release/win-unpacked",
    "dist:mac": "npm run build && electron-builder --mac",
    "verify:package": "node dist/verifyPackagedRelease.js"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
