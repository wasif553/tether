{
  "name": "ses-lockdown",
  "version": "1.3.0",
  "private": true,
  "description": "Tether Secure Browser v1.3.0 (Tether System Check and Exam Readiness v1: adds getClientVersion/getOperatingSystemInfo/getDisplayTopology/getSecureClientCapabilities read-only preload methods, on top of v1.2.2's real verified-session establishment via attestation submission, unified direct-dashboard/protocol launch routing, exam-entry cover from click, and camera capture-failure misclassification fix)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "npm run build && electron .",
    "pack": "npm run build && electron-builder --dir && npm run verify:package",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win && npm run verify:package -- release/win-unpacked",
    "dist:mac": "npm run build && electron-builder --mac",
    "verify:package": "node dist/verifyPackagedRelease.js"
  },
  "dependencies": {
    "electron-store": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
